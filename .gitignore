# Rust
/target/
**/*.rs.bk
*.pdb
Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

.claude/

# Test fixtures (large binaries)
tests/fixtures/plugins/

# Python venv for the audio-correctness judges (see pyproject.toml).
# `uv.lock` is deliberately NOT ignored — it is what makes the judges'
# dependency set reproducible, which is the reason pyproject.toml exists.
.venv/
__pycache__/
IMPROVEMENT_PLAN.md
